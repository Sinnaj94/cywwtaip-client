import lenz.htw.cywwtaip.net.NetworkClient;

/**
 * Idle bot for testing the client
 */
public class IdleBot {
    public static void main(String[] args) {
        new NetworkClient(null, "Idle", "Idle bot won!");
    }
}
